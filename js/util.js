function ParseObject(obj) {
    Object.keys(obj).forEach(function (key) {
        
        if( obj === undefined)
            return;

        if( typeof obj[key] == 'string' )
            ;
        
        if( typeof obj[key] == '')
            ;

    });
}